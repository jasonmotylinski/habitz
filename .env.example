# Habitz unified app — environment variables
# Copy to .env and fill in real values

FLASK_ENV=production

# --- Secret key (shared across all sub-apps) ---
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-shared-fallback

# --- Meal Planner ---
# ANTHROPIC_API_KEY=sk-ant-...
# MEAL_PLANNER_UPLOAD_FOLDER=/var/projects/habitz/uploads

# --- Calorie Tracker ---
# USDA_API_KEY=your-usda-key
# NUTRITIONIX_APP_ID=your-app-id
# NUTRITIONIX_API_KEY=your-api-key

# --- Database (optional override — all apps default to instance/habitz.db) ---
# All apps share a single database. Override only if you need a custom path.
# DATABASE_URL=sqlite:////absolute/path/to/habitz.db
