# Habitz unified app — environment variables
# Copy to .env and fill in real values

FLASK_ENV=production

# --- Secret keys (generate a separate random key for each app) ---
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-shared-fallback

# --- Meal Planner ---
# ANTHROPIC_API_KEY=sk-ant-...
# MEAL_PLANNER_UPLOAD_FOLDER=/var/projects/habitz/uploads

# --- Calorie Tracker ---
# USDA_API_KEY=your-usda-key
# NUTRITIONIX_APP_ID=your-app-id
# NUTRITIONIX_API_KEY=your-api-key

# --- Database overrides (optional — defaults to per-package instance/ dirs) ---
# MEAL_PLANNER_DATABASE_URL=sqlite:////absolute/path/meal_planner.db
# CALORIE_TRACKER_DATABASE_URL=sqlite:////absolute/path/calorie_tracker.db
# FASTING_TRACKER_DATABASE_URL=sqlite:////absolute/path/fasting_tracker.db
# WORKOUT_TRACKER_DATABASE_URL=sqlite:////absolute/path/workout.db
